{
  "name": "patty-wellness-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static site - no build command needed'"
  },
  "dependencies": {
    "@netlify/plugin-lighthouse": "^2.3.0",
    "@netlify/plugin-sitemap": "^1.4.0"
  },
  "engines": {
    "node": "^18 || ^20"
  }
}
